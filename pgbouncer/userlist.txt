; PgBouncer userlist.txt
; Format: "username" "password"
; For md5 auth: password is "md5" + md5(password + username)
; more details https://youtu.be/W-nOdwlxmhA?si=0Zs0CuRhNowDk163
; f.e.
; echo -n "mypasswordpgbouncer" | md5
; (mypassword — password, pgbouncer — user)
; For plain auth: just put the password in quotes
;
; Using scram-sha-256 auth (PostgreSQL 14+) the password can be plain text
; and PgBouncer will hash it automatically when AUTH_TYPE=scram-sha-256

"postgres" "md53175bce1d3201d16594cebf9d7eb3f9d"
